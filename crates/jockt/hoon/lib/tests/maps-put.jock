// Test map put operation
let a = {1 -> 100, 2 -> 200};

// Add new key
let b = a.put(3, 300);

// Update existing key
let c = a.put(1, 999);

// Put into empty map
let d = {->};
let e = d.put(1, 100);

(a b c d e)
