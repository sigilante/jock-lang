// Test set get operation (returns Option)
let a:Set(@) = {1, 2, 3};

// Get existing element (returns [~ val])
let b = a[1];
let c = a[2];

// Get missing element (returns ~)
let d = a[999];

(b c d)
